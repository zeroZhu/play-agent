# ADB Configuration
# Default ADB path (optional, defaults to "adb" if not set)
DEFAULT_ADB_PATH=adb

# Default ADB serial/port for network or USB device (optional)
# Examples: 127.0.0.1:16384 (emulator), emulator-5554, <device-id> (USB)
DEFAULT_ADB_SERIAL=127.0.0.1:16384
